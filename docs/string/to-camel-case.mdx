---
sidebar_position: 1
description: Сonverts a string into `camelCase` format.
authors: [Shchegortzov Artem Mikhailovich]
tags: [string, camel case]
keywords: [string, camel case]
version: 1.2.0
---

import GitHubResourceButton from '../../src/components/mdx/buttons/GitHubResourceButton';
import CodeSandboxResourceButton from '../../src/components/mdx/buttons/CodeSandboxResourceButton';
import LiveFunctionPreview from '../../src/components/LiveFunctionPreview';
import { toCamelCase } from 'shegit-beta';

# toCamelCase

## Using

Сonverts a string into `camelCase` format.

<LiveFunctionPreview
  name="toCamelCase"
  func={toCamelCase}
  args={[{ name: 'str', initial: '"any string"' }]}
/>

### Arguments

| Argument | Type     | Description | Example                            |
| -------- | -------- | ----------- | ---------------------------------- |
| `str`    | `string` | String      | `"iAmString"` <br /> `"Text_TEXT"` |

### Returns

`string` | `undefined` — String in the format `camelCase` or `undefined` if `str` was not a string

### Examples

```js
toCamelCase('some value');
// "someValue"

toCamelCase('ANY_STRING');
// "anyString"

toCamelCase('iLikeShegit');
// "iLikeShegit"

toCamelCase();
// undefined

toCamelCase(55);
// undefined
```

### Since

[1.2.0](https://github.com/or-temka/shegit/tree/dev-v1.2.0)

## Resources

<div style={{ display: 'flex', gap: 20 }}>
  <GitHubResourceButton link="https://github.com/or-temka/shegit/tree/dev-v1.2.0/src/functions/string/toCamelCase" />
  {/* <CodeSandboxResourceButton link="https://codesandbox.io/p/sandbox/rgbarraytoobj-rppnwn" /> */}
</div>
