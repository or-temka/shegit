---
sidebar_position: 1
description: Function selects random items from array, object, map, set.
authors: [Shchegortzov Artem Mikhailovich]
tags: [random, select]
keywords: [random, select, array, object, set, map]
version: 1.2.0
---

import GitHubResourceButton from '../../src/components/mdx/buttons/GitHubResourceButton';
import CodeSandboxResourceButton from '../../src/components/mdx/buttons/CodeSandboxResourceButton';
import LiveFunctionPreview from '../../src/components/LiveFunctionPreview';
import { getRandomValue } from 'shegit-beta';

# getRandomValue

## Using

Function selects random items from array, object, map, set. If a primitive, it just returns the primitive as it is.

<LiveFunctionPreview
  name="getRandomValue"
  func={getRandomValue}
  args={[{ name: 'input', initial: '["value1", "value2", 3, "4", true, false]' }]}
/>

### Arguments

| Argument | Type  | Description     | Example                                                               |
| -------- | ----- | --------------- | --------------------------------------------------------------------- |
| `input`  | `any` | Can be any type | `["Array"]` <br /> `{ name: "Alina" }` <br /> `new Map()` <br /> `14` |

### Returns

`any` — selected random value from `input` or `input` for primitive.

### Examples

```js
getRandomValue([1, 2, 3]);
// 2

getRandomValue([1, 2, 3]);
// 3

getRandomValue({ name: 'Alina', age: 20, sex: 'w' });
// "Alina"

getRandomValue({ name: 'Alina', age: 20, sex: 'w' });
// 20

getRandomValue(66);
// 66

getRandomValue('text');
// "text"
```

### Since

[1.2.0](https://github.com/or-temka/shegit/tree/dev-v1.2.0)

## Resources

<div style={{ display: 'flex', gap: 20 }}>
  <GitHubResourceButton link="https://github.com/or-temka/shegit/tree/dev-v1.2.0/src/functions/util/getRandomValue" />
  {/* <CodeSandboxResourceButton link="https://codesandbox.io/p/sandbox/rgbarraytoobj-rppnwn" /> */}
</div>
