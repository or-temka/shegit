---
sidebar_position: 3
description: This function determines whether the given value is exactly null.
authors: [Shchegortzov Artem Mikhailovich]
tags: [is, 'null', falsy]
keywords: [is, 'null', falsy]
version: 1.3.0
---

import GitHubResourceButton from '../../src/components/mdx/buttons/GitHubResourceButton';
import CodeSandboxResourceButton from '../../src/components/mdx/buttons/CodeSandboxResourceButton';
import LiveFunctionPreview from '../../src/components/LiveFunctionPreview';
import { isNull } from 'shegit-beta';

# isNull

## Using

This function determines whether the given value is exactly `null`.

- Returns `true` only for the `null` literal.
- Returns `false` for any other value, including `undefined`, `false`, `0`, `""`, `NaN`, objects, arrays, and other falsy types.

<LiveFunctionPreview
  name="isNull"
  func={isNull}
  args={[{ name: 'value', initial: 'null', style: { width: 100 } }]}
/>

### Arguments

| Argument | Type      | Description     | Example                                |
| -------- | --------- | --------------- | -------------------------------------- |
| `value`  | `unknown` | Can be any type | `"text"` <br /> `2` <br /> `undefined` |

### Returns

`boolean` — `true` if value is `null`, `false` otherwise

### Examples

```js
isNull(null);
// true

isNull(undefined);
// false

isNull(0);
// false

isNull('');
// false

isNull({});
// false

isNull(Object.create(null));
// false

isNull(() => null);
// false
```

### Since

[1.3.0](https://github.com/or-temka/shegit/tree/dev-v1.3.0)

## Resources

<div style={{ display: 'flex', gap: 20 }}>
  <GitHubResourceButton link="https://github.com/or-temka/shegit/blob/dev-v1.3.0/src/functions/util/isNull" />
  {/* <CodeSandboxResourceButton link="https://codesandbox.io/p/sandbox/rgbarraytoobj-rppnwn" /> */}
</div>
