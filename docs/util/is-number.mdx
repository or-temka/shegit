---
sidebar_position: 3
description: This function determines whether the given value is a valid, finite primitive number.
authors: [Shchegortzov Artem Mikhailovich]
tags: [is, number, falsy]
keywords: [is, number, falsy]
---

import LiveFunctionPreview from '../../src/components/LiveFunctionPreview';
import Resources from '../../src/widgets/Resources';
import { FUNCTIONS_CONFIG } from './constants';
import { isNumber } from 'shegit';

export const constants = FUNCTIONS_CONFIG['isNumber'];

# isNumber

## Using

This function determines whether the given value is a valid, finite primitive number.

- Returns `true` if the value is a finite number (not `NaN`, `Infinity`, or `-Infinity`).
- Returns `false` for non-number types or special numeric values.

<LiveFunctionPreview
  name="isNumber"
  func={isNumber}
  args={[{ name: 'value', initial: '10', style: { width: 100 } }]}
/>

### Arguments

| Argument | Type      | Description     | Example                                |
| -------- | --------- | --------------- | -------------------------------------- |
| `value`  | `unknown` | Can be any type | `"text"` <br /> `2` <br /> `undefined` |

### Returns

`boolean` — `true` if value is a finite number, `false` otherwise

### Examples

```js
isNumber(42);
// true

isNumber(0);
// true

isNumber(-3.14);
// true

isNumber('42');
// false

isNumber(NaN);
// false

isNumber(Infinity);
// false

isNumber(new Number(5));
// false

isNumber(() => {});
// false

isNumber(undefined);
// false
```

## Resources

<Resources constants={constants} />
