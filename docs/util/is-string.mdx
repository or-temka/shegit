---
sidebar_position: 3
description: This function determines whether the given value is a primitive string.
authors: [Shchegortzov Artem Mikhailovich]
tags: [is, string, falsy]
keywords: [is, string, falsy]
---

import GitHubResourceButton from '../../src/components/mdx/buttons/GitHubResourceButton';
import CodeSandboxResourceButton from '../../src/components/mdx/buttons/CodeSandboxResourceButton';
import LiveFunctionPreview from '../../src/components/LiveFunctionPreview';
import Versions from '../../src/components/mdx/Versions';
import { FUNCTIONS_CONFIG } from './constants';
import { isString } from 'shegit';

export const constants = FUNCTIONS_CONFIG['isString'];

# isString

## Using

This function determines whether the given value is a **primitive string**.

- Returns `true` if the value is a primitive string (e.g., `"text"`, `''`, template literals).
- Returns `false` for any non-string type or string object (e.g., `new String("text")`).

<LiveFunctionPreview
  name="isString"
  func={isString}
  args={[{ name: 'value', initial: '"string"', style: { width: 100 } }]}
/>

### Arguments

| Argument | Type      | Description     | Example                                |
| -------- | --------- | --------------- | -------------------------------------- |
| `value`  | `unknown` | Can be any type | `"text"` <br /> `2` <br /> `undefined` |

### Returns

`boolean` — `true` if value is a string primitive, `false` otherwise

### Examples

```js
isString('hello');
// true

isString('');
// true

isString(42);
// false

isString(new String('hello'));
// false

isString(null);
// false

isString(() => 'string');
// false
```

## Resources

<Versions {...constants.versions} />

<div style={{ display: 'flex', gap: 20 }}>
  <GitHubResourceButton linkPath={constants.resourceLinkPath} />
</div>
