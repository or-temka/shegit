---
sidebar_position: 3
description: Convert CMYK color as an object to a format array
authors: [Shchegortzov Artem Mikhailovich]
tags: [object, array, color, cmyk]
keywords: [shegit, cmyk, array, object, color]
version: 1.1.0
---

import LiveFunctionPreview from '../../../../src/components/LiveFunctionPreview';
import Resources from '../../../../src/widgets/Resources';
import { FUNCTIONS_CONFIG } from '../../../../docs/colors/utility/constants';
import { cmykObjToArray } from 'shegit1.1.0';

export const constants = FUNCTIONS_CONFIG['cmykObjToArray'];

# cmykObjToArray

## Using

Convert CMYK color `{c: cyan, m: magenta, y: yellow, k: key }` as an object to a format array `[cyan, magenta, yellow, key]`

<LiveFunctionPreview
  name="cmykObjToArray"
  func={cmykObjToArray}
  args={[{ name: 'obj', default: '{"c": 255, "m": 14, "y": 96, "k": 59}' }]}
/>

### Arguments

| Argument | Type                       | Description        | Example                        |
| -------- | -------------------------- | ------------------ | ------------------------------ |
| `obj`    | `object` <br /> `function` | CMYK color object. | `{c: 10, m: 24, y: 57, k: 96}` |

### Returns

`array` — Returns CMYK array in format `[cyan, magenta, yellow, key]`.

### Examples

```js
cmykObjToArray({ c: 15, m: 76, y: 14, k: 50 });
// [15, 76, 14, 50]

cmykObjToArray({ c: '+99', m: 300, y: -5, k: 99 });
// [99, 100, 0, 99]

cmykObjToArray({ c: () => true, m: false, y: 45.9, k: NaN });
// [1, 0, 45, 0]

cmykObjToArray(() => ({ c: 3.2, m: () => 55, y: '84', k: '-7' }));
// [3, 55, 84, 0]
```

## Resources

<Resources constants={constants} />
