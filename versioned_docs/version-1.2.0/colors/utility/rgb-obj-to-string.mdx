---
sidebar_position: 1
description: Convert RGB color as an object to a format string
authors: [Shchegortzov Artem Mikhailovich]
tags: [object, string, color, rgb]
keywords: [shegit, rgb, string, object, color]
version: 1.1.0
---

import GitHubResourceButton from '../../../../src/components/mdx/buttons/GitHubResourceButton';
import CodeSandboxResourceButton from '../../../../src/components/mdx/buttons/CodeSandboxResourceButton';
import LiveFunctionPreview from '../../../../src/components/LiveFunctionPreview';
import { FUNCTIONS_CONFIG } from '../../../../docs/colors/utility/constants';
import { rgbObjToString } from 'shegit1.2.0';

export const constants = FUNCTIONS_CONFIG['rgbObjToString'];

# rgbObjToString

## Using

Convert RGB color `{r: red, g: green, b: blue}` as an object to a format string `"rgb(red, green, blue)"`

<LiveFunctionPreview
  name="rgbObjToString"
  func={rgbObjToString}
  args={[{ name: 'obj', initial: '{"r": 199, "g": 85, "b": 4}' }]}
/>

### Arguments

| Argument | Type                       | Description       | Example                    |
| -------- | -------------------------- | ----------------- | -------------------------- |
| `obj`    | `object` <br /> `function` | Rgb color object. | `{r: 209, g: 205, b: 152}` |

### Returns

`string` — Returns RGB string in format `"rgb(red, green, blue)"` (`red`, `green`, `blue` = number).

### Examples

```js
rgbObjToString({ r: 15, g: 76, b: 213 });
// rgb(15, 76, 213)

rgbObjToString({ r: '+99', g: 300, b: -5 });
// rgb(99, 255, 0)

rgbObjToString({ r: () => true, g: false, b: 245.9 });
// rgb(1, 0, 245)

rgbObjToString(() => ({ r: 3.2, g: () => 55, b: '184' }));
// rgb(3, 55, 184)
```

### Since

[1.1.0](https://www.npmjs.com/package/shegit/v/1.1.0)

## Resources

<div style={{ display: 'flex', gap: 20 }}>
  <GitHubResourceButton linkPath={constants.resourceLinkPath} />
</div>
