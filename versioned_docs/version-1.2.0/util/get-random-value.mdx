---
sidebar_position: 1
description: Function selects random items from array, object, map, set.
authors: [Shchegortzov Artem Mikhailovich]
tags: [random, select]
keywords: [random, select, array, object, set, map]
version: 1.2.0
---

import LiveFunctionPreview from '../../../src/components/LiveFunctionPreview';
import Resources from '../../../src/widgets/Resources';
import { FUNCTIONS_CONFIG } from '../../../docs/util/constants';
import { getRandomValue } from 'shegit1.2.0';

export const constants = FUNCTIONS_CONFIG['getRandomValue'];

# getRandomValue

## Using

Function selects random items from array, object, map, set. If a primitive, it just returns the primitive as it is.

<LiveFunctionPreview
  name="getRandomValue"
  func={getRandomValue}
  args={[
    { name: 'input', initial: '["value1", "value2", 3, "4", true, false]', style: { width: 400 } },
  ]}
/>

### Arguments

| Argument | Type  | Description     | Example                                                               |
| -------- | ----- | --------------- | --------------------------------------------------------------------- |
| `input`  | `any` | Can be any type | `["Array"]` <br /> `{ name: "Alina" }` <br /> `new Map()` <br /> `14` |

### Returns

`any` — selected random value from `input` or `input` for primitive.

### Examples

```js
getRandomValue([1, 2, 3]);
// 2

getRandomValue([1, 2, 3]);
// 3

getRandomValue({ name: 'Alina', age: 20, sex: 'w' });
// "Alina"

getRandomValue({ name: 'Alina', age: 20, sex: 'w' });
// 20

getRandomValue(66);
// 66

getRandomValue('text');
// "text"
```

## Resources

<Resources constants={constants} />
