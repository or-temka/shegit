---
sidebar_position: 3
description: Convert CMYK color as an object to a format string
authors: [Shchegortzov Artem Mikhailovich]
tags: [object, string, color, cmyk]
keywords: [shegit, cmyk, string, object, color]
version: 1.1.0
---

import GitHubResourceButton from '../../../../src/components/mdx/buttons/GitHubResourceButton';
import CodeSandboxResourceButton from '../../../../src/components/mdx/buttons/CodeSandboxResourceButton';
import LiveFunctionPreview from '../../../../src/components/LiveFunctionPreview';
import Versions from '../../../../src/components/mdx/Versions';
import { FUNCTIONS_CONFIG } from '../../../../docs/colors/utility/constants';
import { cmykObjToString } from 'shegit1.3.0';

export const constants = FUNCTIONS_CONFIG['cmykObjToString'];

# cmykObjToString

## Using

Convert CMYK color `{c: cyan, m: magenta, y: yellow, k: key }` as an object to a format string `'cmyk(cyan%, magenta%, yellow%, key%)'`

<LiveFunctionPreview
  name="cmykObjToString"
  func={cmykObjToString}
  args={[{ name: 'obj', initial: '{"c": 255, "m": 14, "y": 96, "k": 59}' }]}
/>

### Arguments

| Argument | Type                       | Description                    | Example                        |
| -------- | -------------------------- | ------------------------------ | ------------------------------ |
| `obj`    | `object` <br /> `function` | CMYK color object or function. | `{c: 10, m: 24, y: 57, k: 96}` |

### Returns

`string` — Returns CMYK string in format `'cmyk(cyan%, magenta%, yellow%, key%)'`.

### Examples

```js
cmykObjToString({ c: 15, m: 76, y: 14, k: 50 });
// [15, 76, 14, 50]

cmykObjToString({ c: '+99', m: 300, y: -5, k: 99 });
// [99, 100, 0, 99]

cmykObjToString({ c: () => true, m: false, y: 45.9, k: NaN });
// [1, 0, 45, 0]

cmykObjToString(() => ({ c: 3.2, m: () => 55, y: '84', k: '-7' }));
// [3, 55, 84, 0]
```

## Resources

<Resources constants={constants} />
