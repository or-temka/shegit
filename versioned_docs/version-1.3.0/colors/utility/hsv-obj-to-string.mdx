---
sidebar_position: 4
description: Convert HSV color as an object to a format string
authors: [Shchegortzov Artem Mikhailovich]
tags: [object, string, color, hsv]
keywords: [shegit, hsv, string, object, color]
version: 1.1.0
---

import GitHubResourceButton from '../../../../src/components/mdx/buttons/GitHubResourceButton';
import CodeSandboxResourceButton from '../../../../src/components/mdx/buttons/CodeSandboxResourceButton';
import LiveFunctionPreview from '../../../../src/components/LiveFunctionPreview';
import Versions from '../../../../src/components/mdx/Versions';
import { FUNCTIONS_CONFIG } from '../../../../docs/colors/utility/constants';
import { hsvObjToString } from 'shegit1.3.0';

export const constants = FUNCTIONS_CONFIG['hsvObjToString'];

# hsvObjToString

## Using

Convert HSV color `{h: hue, s: saturation, v: value}` as an object to a format array "hsv(`hue`, `saturation`%, `value`%)".

<LiveFunctionPreview
  name="hsvObjToString"
  func={hsvObjToString}
  args={[{ name: 'obj', initial: '{"h": 220, "s": 67, "v": 96}' }]}
/>

### Arguments

| Argument | Type                       | Description                   | Example                  |
| -------- | -------------------------- | ----------------------------- | ------------------------ |
| `obj`    | `object` <br /> `function` | HSV color object or function. | `{h: 300, s: 10, v: 45}` |

### Returns

`string` — Returns HSV string in format "hsv(`hue`, `saturation`%, `value`%)".

### Examples

```js
hsvObjToString({ h: -22, s: 99, v: '39' });
// "hsv(0, 99%, 39%)"

hsvObjToString({ h: () => 2, s: () => 55, v: () => 1005 });
// "hsv(2, 55%, 100%)"

hsvObjToString('some');
// "hsv(0, 0%, 0%)"
```

## Resources

<Versions {...constants.versions} />

<div style={{ display: 'flex', gap: 20 }}>
  <GitHubResourceButton linkPath={constants.resourceLinkPath} />
  <CodeSandboxResourceButton link={constants.codeSandboxLink} />
</div>
