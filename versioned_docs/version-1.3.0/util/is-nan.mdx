---
sidebar_position: 3
description: This function determines whether the given value is the special NaN value.
authors: [Shchegortzov Artem Mikhailovich]
tags: [is, NaN, falsy]
keywords: [is, NaN, falsy]
version: 1.3.0
---

import LiveFunctionPreview from '../../../src/components/LiveFunctionPreview';
import Resources from '../../../src/widgets/Resources';
import { FUNCTIONS_CONFIG } from '../../../docs/util/constants';
import { isNaN } from 'shegit1.3.0';

export const constants = FUNCTIONS_CONFIG['isNaN'];

# isNaN

## Using

This function determines whether the given value is the special `NaN` value (Not-a-Number).

- Returns `true` only if the value is the **primitive** `NaN`.
- Returns `false` for all other values, including anything that is not strictly the `NaN` primitive.

Unlike the global `isNaN()` function, this version avoids type coercion.

<LiveFunctionPreview
  name="isNaN"
  func={isNaN}
  args={[{ name: 'value', initial: 'NaN', style: { width: 100 } }]}
/>

### Arguments

| Argument | Type      | Description     | Example                                |
| -------- | --------- | --------------- | -------------------------------------- |
| `value`  | `unknown` | Can be any type | `"text"` <br /> `2` <br /> `undefined` |

### Returns

`boolean` — `true` if value is primitive `NaN`, `false` otherwise

### Examples

```js
isNaN(NaN);
// true

isNaN(0 / 0);
// true

isNaN(Number('abc'));
// true

isNaN('NaN');
// false

isNaN(42);
// false

isNaN(new Number(NaN));
// false

isNaN(() => NaN);
// false
```

## Resources

<Resources constants={constants} />
